# Atualize o código do GitHub
git pull

# Execute o build do aplicativo (substitua 'yarn build' pelo comando apropriado)
yarn build

# Reinicie o PM2 (substitua 'pm2 restart' pelo comando apropriado)
pm2 restart server